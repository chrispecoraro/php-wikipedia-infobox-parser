A Laravel command to fetch and parse infobox contents.

Returns a json-encoded object with the key-values inside the infobox.

syntax: http://{url}/infobox/{page title}

example: http://myurl.com/infobox/U2
 